<%= form_with(model: commande) do |form| %>
  <% if commande.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(commande.errors.count, "error") %> prohibited this commande from being saved:</h2>

      <ul>
        <% commande.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nom, style: "display: block" %>
    <%= form.text_field :nom %>
  </div>

  <div>
    <%= form.label :client_id, style: "display: block" %>
    <%= form.select :client_id, Client.all.collect { |c| [ c.nom, c.id ] }, include_blank: true %>
  </div>

  <div>
    <%= form.label :montant, style: "display: block" %>
    <%= form.text_field :montant, value: 1 %>
    
  </div>

  <div> <br>
    <%= form.submit 'Valider', class:'btn btn-sm btn-success' %>
  </div>
<% end %>
