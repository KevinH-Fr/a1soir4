
  <div class="table-responsive">
    <table class="table">
      <th> <%#= image_tag("logo.jpg", border: 0, width: 60) %> </th>
      <% if @label.present? %> <%# message label paramétré par user %>
        <hr> <th>  <%= render "labelPrincipale" %></th>   
      <% end %>
    </table>
  </div>

  <div class="d-flex align-items-center text-center" style="min-height: 45vh">
    <div class="box w-100">
      <%= render "accueil/slideshow" %>
    </div>
  </div>

   <%= render "accueil/footer" %>

