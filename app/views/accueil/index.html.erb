

  <div class="container-fluid m-0 p-0">
    <%= render partial: "shared/navbarPublic" %>
  </div>

  <div class="table-responsive">
    <table class="table">
      <th> <%#= image_tag("logo.jpg", border: 0, width: 60) %> </th>
        <% if @label.present? %> <%# message label paramétré par user %>
      <th>  <%= render "labelPrincipale" %></th>   
        <% end %>
      </table>
  </div>

  <div class="d-flex align-items-center text-center" style="min-height: 65vh">
    <div class="box w-100">
      <%= render "accueil/slideshow" %>
    </div>
  </div>

