
<%= form_with(model: paiement) do |form| %>
  <% if paiement.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(paiement.errors.count, "error") %> 
              prohibited this paiement from being saved:</h2>
      <ul>
        <% paiement.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <div class="card-body with-shadow m-2">
      <div class="input-group mb-3">
        <%#= form.label :commande_id, class:"input-group-text bg-secondary text-light", style:"width: 100px;"%>
        <%= form.hidden_field :commande_id, value: session[:commandeId], class:"form-control" %> 
      </div>
      <div class="input-group mb-3">
        <%= form.label :type, class:"input-group-text bg-secondary text-light", style:"width: 100px;"%>
        <%= form.select :typepaiement, Paiement.typePaiements.keys, {include_blank: true}, 
            class:"form-control" %> 
      </div>
      <div class="input-group mb-3">
        <%= form.label :montant, class:"input-group-text bg-secondary text-light", style:"width: 100px;"%>
        <%= form.text_field :montant, class:"form-control" %> 
      </div>
      <div> 
        <%= form.submit "Créer", class: 'btn btn-success' %>
      </div>
  </div>
<% end %>