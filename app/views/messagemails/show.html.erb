<% if user_signed_in? %>


    <div class="bloc-entete-model">
      <div class="entete-show">
        <strong>Mail à envoyer</strong> 
      </div>
    </div>

    <div class="">
      <%= render @messagemail %>
    </div>

      <div class="text-end">
        <%= link_to "envoyer mail", send_client_mail_client_path(messagemailId: @messagemail, clientId: @messagemail.client_id), 
            class:"btn bg-success btn-sm btn-dark" %>
      </div>
   
  <hr>

  <div>
    <%= link_to "Edit this messagemail", edit_messagemail_path(@messagemail) %> |
    <%= link_to "Back to messagemails", messagemails_path %>

    <%= button_to "Destroy this messagemail", @messagemail, method: :delete %>
  </div>

<% end %>