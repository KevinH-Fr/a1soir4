<%= form_with(model: client) do |form| %>
  <% if client.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(client.errors.count, "error") %> prohibited this client from being saved:</h2>
      <ul>
        <% client.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="card-body with-shadow">
      <div class="input-group mb-3">
        <%= form.label :nom, class:"input-group-text bg-secondary text-light", style:"width: 100px;" %>
        <%= form.text_field :nom, class:"form-control border border-secondary" %>
      </div>

      <div class="input-group mb-3">
        <%= form.label :mail, class:"input-group-text bg-secondary text-light", style:"width: 100px;" %>
        <%= form.text_field :mail, class:"form-control border border-secondary" %>
      </div>

      <div class="input-group mb-3">
        <%= form.label :commentaires, class:"input-group-text bg-secondary text-light", style:"width: 100px;" %>
        <%= form.text_area :commentaires, class:"form-control border border-secondary" %>
      </div>
      <%= form.submit "Valider", class: "btn btn-primary" %>
  </div>

<% end %>