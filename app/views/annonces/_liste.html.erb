
 <table class="table table-secondary table-striped table-bordered table-hover">
      <thead class="table-dark">
        <tr>
          <th style="text-align: center;">#</th>
          <th>principale</th>
          <th colspan=3> Actions</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <% @annonces.each do | annonce | %>
            <td> <%= annonce.id %> </td>
            <td> <%= annonce.principale %> </td>
            <td> <%= link_to '', client, class:"fa-solid fa-eye btn btn-primary btn-sm" %>  </td>
            <td> <%= button_to '', client, class:"fa-solid fa-trash-can btn btn-danger btn-sm", method: :delete, :onclick => "return confirm('Sûr ?')" %>  </td> 
            <td> <%= link_to "", edit_client_path(client), class:"fa-regular fa-pen-to-square btn btn-secondary btn-sm" %> </td> 
        </tr> 
        <% end %>
      </tbody>
    </table>